import { DropdownMenuItem } from '@/shared/ui/dropdown-menu';

const BlockButton = () => {
	return <DropdownMenuItem className="cursor-pointer">Block</DropdownMenuItem>;
};

export default BlockButton;
