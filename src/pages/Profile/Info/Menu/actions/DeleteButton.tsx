import { DropdownMenuItem } from '@/shared/ui/dropdown-menu';

const DeleteButton = () => {
	return <DropdownMenuItem className="cursor-pointer">Delete</DropdownMenuItem>;
};

export default DeleteButton;
